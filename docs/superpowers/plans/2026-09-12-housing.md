# Housing Decision Workspace Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. The user has already selected delegated work with Astra coordinating, inexpensive bounded workers, Fable frontend review and a backend reviewer; do not ask them to choose an execution method again. Steps use checkbox syntax for tracking.

**Goal:** Deliver a sourced, refreshable CMU housing search that compares the user's real rent share, utility uncertainty, Gates Hillman walk, useful transit and everyday context, then demonstrates what a changed preference unlocks.

**Architecture:** One React app and local Node API share a deterministic TypeScript decision engine. Bounded discovery, source parsing and geographic jobs publish immutable evidence-backed snapshots; the interface keeps working from the last good snapshot when external services fail. Model interpretation assists acquisition but cannot replace evidence, arithmetic or hard-constraint checks.

**Tech Stack:** React, TypeScript, Vite, Express, Zod, Cheerio, Leaflet, csv-parse, fflate; Vitest and browser automation. Local authenticated Claude/OpenCode CLIs for bounded research/extraction/review. One npm lockfile, no database or cloud-service prerequisite.

**Spec:** [2026-09-12-housing-design.md](../specs/2026-09-12-housing-design.md), [canonical contracts](../../CONTRACTS.md), [requirements C01–C22](../../context/DECISIONS.md).

## Global constraints

- Implementation is authorized; continue autonomously through the complete application and review cycle.
- Review target: approximately 2026-09-12 11:00 America/New_York (15:00 UTC), with quality prioritized; submission: 16:00 EDT (20:00 UTC).
- Default: personal rent 120000 cents/month, 2 equal shares explicitly assumed, 2 bedrooms, minimum 2 advertised bathrooms, houses/apartments, walking duration <=1200 seconds, flexible timing.
- Destination: Gates Hillman — mapped entrance, latitude 40.4440338, longitude -79.9445593; retain entrance uncertainty and source evidence.
- Unknown is not zero; unknown hard criteria do not pass; unknown utilities do not fail the rent-only budget.
- Use real sourced seed records; no invented qualifying inventory, no silent relaxation, no unsupported exhaustive-coverage claim.
- Preserve coverage, consequences, alternatives, readiness, browsable lists and sourced broader life context.
- One canonical TypeScript contract/engine; money in USD integer cents, time in seconds, coordinates named lat/lon except GeoJSON [lon, lat], timestamps ISO UTC.
- One root lockfile; Node >=22.12, validated local runtime 26.8.1; API binds 127.0.0.1 by default.
- All model/source/routing jobs are bounded; keep the last good snapshot on failure; never bypass source access controls.

## Execution order, ownership and time

Read the actual clock at go. Read PROJECT.md, this spec/contract, the assigned task and `hackcmu-intel/strategy/PLAYBOOK.md`, `strategy/LESSONS.md`, `execution/OVERNIGHT.md` before execution. Preserve the supplied intel archive and any human edits. Root establishes the baseline/isolated worktrees at execution time using the worktree skill, then serializes integration. If a worktree mechanism fails, use isolated checkouts with explicit file ownership; do not spend a long cycle repairing coordination tooling.

| Milestone | Relative target from go | Observable deliverable |
| --- | --- | --- |
| A | 0–25 min | Contracts, scripts, seed criteria, app/API shell; workers started with owned files |
| B | 25–70 min | First real imported snapshot and working budget/layout engine; styled list using actual records |
| C | 70–130 min | List/map/selected route, costs/utilities, source ledger and honest alternatives connected |
| D | 130–200 min | Broader seed, PRT/errands, comparison/shortlist, second real refresh, first Fable visual corrections |
| E | 200–250 min | Integrated acceptance and failure checks, final design/backend review, startup and demo handoff |

Keep a runnable candidate by 9 a.m. EDT if go timing permits. At 9:30 a.m. stop optional expansion and preserve the working candidate; complete defects that prevent the required viewing flow. If go occurs late, prioritize the three required outcomes and surface the timing impact immediately; do not silently reset the 10 a.m. target. Never cut rent/utility truth, source provenance, actual Gates routing or honest empty states to add spectacle.

**Acquisition gate at T+45 minutes:** count real addressed records, independent organizations and captured rent/layout evidence. If fewer than 12 useful addressed records across 3 organizations are available, root reallocates one worker from optional visual flourishes/multi-change suggestions to source research. Freeze generic portal adapters; use only the proven CMU/direct-manager paths, retain other sources as surveyed/link-only gaps, and continue an agent-assisted import through the same pipeline. Reassess at T+75 once. Any remaining seed shortfall is an explicit release defect and gets priority over optional features; it never authorizes synthetic housing inventory or a false coverage claim.

**Parallel lanes:** Root owns shared schemas, dependencies, API, integration and final acceptance. Terra owns the engine, then geographic enrichment. Luna owns bounded source adapters/seed work. Sol or Terra owns frontend implementation depending on free slots, with Fable as the independent external reviewer. At most three native workers alongside root. New bounded Luna agents can take source auditing or accessibility/copy tasks after a lane finishes. Assign fresh tasks instead of creating a permanent swarming panel.

**Review bounds:** Fable's planning review already exists; consult it again on the first rendered core and final candidate. OpenCode Go performs one integrated backend review after data/API/engine work. Each report names at most five important findings, one repair owner, and one targeted verification. Retry a failing command/approach at most twice without new evidence, then use its declared fallback. Do not rerun a full suite just for reassurance after no code/data changed.

**User's explicit quality emphasis:** the next phase is the full working application with fresh real data from several selected housing sites. Frontend craft is roughly half the hackathon battle. Budget the frontend lane across implementation, rendered review, revision and final inspection; do not treat it as decoration added after backend completion. A functional first pass alone does not satisfy the visual acceptance requirement.

## File map

| Owner | Paths | Responsibility |
| --- | --- | --- |
| Root | `package.json`, `package-lock.json`, `tsconfig*.json`, `vite.config.ts`, `index.html`, `.gitignore` | Dependencies, scripts, compiler and dev API proxy |
| Root | `src/domain/schema.ts`, `src/config/seed.ts`, `src/config/presentation.ts` | Exact contracts, initial search, adjustable naming/layout |
| Terra | `src/domain/costs.ts`, `matching.ts`, `alternatives.ts`, `reconcile.ts`, `coverage.ts` | Pure money/criteria/alternatives/deduplication/research scope |
| Luna | `jobs/sources/registry.ts`, `public-page.ts`, `cmu.ts`, `lobos.ts`, `manager-page.ts`, `extract.ts`, `discover.ts`, `ingest.ts` | Discovery, captured evidence, unit-scoped parsing and seed generation |
| Terra, after engine | `jobs/geo/geocode.ts`, `walk.ts`, `transit.ts`, `nearby.ts`, `enrich.ts` | Destination coordinates, route caches, PRT and OSM context |
| Root | `server/index.ts`, `api.ts`, `snapshots.ts`, `jobs.ts`, `cli.ts` | Serving, job orchestration, subprocess limits, atomic publication |
| Frontend worker | `src/main.tsx`, `App.tsx`, `state/search.ts`, `lib/api.ts`, `components/*`, `styles/*` | App, scenario state, list/map/details/comparison/coverage |
| Assigned producer | `tests/domain/*`, `tests/ingest/*`, `tests/geo/*`, `tests/api/*` | Meaningful boundary/integrity tests |
| Root | `tests/browser/*`, `scripts/demo-reset.ts`, `docs/runbook.md`, `docs/demo.md`, `docs/verification.md` | End-to-end acceptance and reproducible handoff |
| Data lane; root promotes | `data/snapshots/<id>/snapshot.json`, `data/current.json`, `data/seed/criteria.json` | Auditable snapshot and seed; `data/raw`, private CLI logs and caches ignored |

The frontend worker owns separate components: `SearchHeader`, `CriteriaEditor`, `CoverageStrip`, `CoveragePanel`, `HomeList`, `HomeRow`, `UtilityStrip`, `HousingMap`, `HomeDetail`, `Alternatives`, `CompareSheet`, `ShortlistRail`, `SourceEvidence`, `DiscoveryStatus`. Prefer a component only when it has distinct behavior or repeated structure; no empty wrappers.

## Task 1: Contract and runnable foundation

**Owner:** Root. **Depends on:** final go. **Produces:** buildable empty shell, shared types and commands. Workers can start source research concurrently, but cannot invent their own contracts.

**Files:** root configuration; `src/domain/schema.ts`; `src/config/seed.ts`; `src/config/presentation.ts`; `tests/domain/schema.test.ts`; a minimal `server/index.ts` and `src/main.tsx` to be handed to their owners.

**Interfaces:** Implement every type named in CONTRACTS.md as a Zod schema/inferred type. Export `SnapshotSchema`, `CriteriaSchema`, `CriteriaPatchSchema`, `validateSnapshot`, `SEED_CRITERIA`. The root creates the public shared interface once; all producers use it.

- [ ] Record go time, baseline Git state and ownership. Create the root-owned configuration without overwriting existing docs/intel. Pin one resolved dependency set using npm; inspect installer/engine errors rather than guessing versions. Install browser verification support early if the in-app browser remains unavailable.
- [ ] Set these scripts; `build` must compile the server as well as the frontend, and `start` must work from the built output:

```json
{
  "dev": "concurrently -k \"vite --host 127.0.0.1\" \"tsx watch server/index.ts\"",
  "typecheck": "tsc -b --pretty false",
  "test": "vitest run",
  "build": "npm run typecheck && vite build && tsc -p tsconfig.server.json",
  "start": "node dist-server/server/index.js",
  "ingest": "tsx jobs/ingest.ts",
  "enrich": "tsx jobs/geo/enrich.ts",
  "demo:reset": "tsx scripts/demo-reset.ts",
  "test:browser": "playwright test"
}
```

- [ ] Use `type: module`, include Node typings, configure compiled server output to retain its module paths, configure Vite `/api` proxy to `http://127.0.0.1:4318`, and use port 4173 for the production demo. In production Express serves the Vite `dist` directory; source/raw files are not public. Document the exact resolved commands, not an assumed dev-only path.
- [ ] Encode seed criteria from the spec. Validate evidence references, money safe integers, ranges, unique IDs, lat/lon limits, timestamps and route references. Known sourced values require evidence; unknowns retain null. Do not require unknown fees to become zero.
- [ ] Write and run a narrow schema test: invalid negative rent, duplicate home ID, missing evidence reference, incoherent equal-share fraction and GeoJSON coordinate reversal fail. Synthetic test data stays under `tests/fixtures/` and is labeled.

```ts
expect(CriteriaSchema.safeParse({ ...SEED_CRITERIA,
  allocation: { occupants: 2, kind: 'equal', personalShareBps: 9000 }
}).success).toBe(false);
expect(CriteriaSchema.parse(SEED_CRITERIA).personalRentCap).toBe(120000);
```

- [ ] Run `npm run typecheck`, the schema test, and `npm run build`; open the shell in a browser. This proves toolchain/browser access only, not product completion. Commit an explicit safe baseline and pass the types to workers.

## Task 2: Deterministic costs, matches, alternatives and reconciliation

**Owner:** Terra. **Depends on:** Task 1 contract. **Files:** domain files in map, `tests/domain/{costs,matching,alternatives,reconcile}.test.ts`, `tests/fixtures/homes.ts`.

**Consumes:** `Home`, `Snapshot`, `Criteria`, `WalkRoute`, `ResearchScope`. **Produces:** all pure functions in CONTRACTS.md other than root-owned `validateSnapshot`; frontend and API import these exact exports. No network or model calls inside this lane.

- [ ] Create a labeled synthetic fixture factory with a $2,400 base-rent whole unit, exactly 2 bedrooms, 2 bathrooms, 1,200-second foot route to the seed destination, unknown electricity and separately stated $20/person monthly fee. Attach internally consistent synthetic evidence; use explicit `test:` IDs. Implement the factory completely, not as a cast over missing required fields.
- [ ] Write boundary tests before computation. These are the minimum decisive assertions:

```ts
expect(computeCosts(home2400, SEED_CRITERIA).personalBaseRent).toBe(120000);
expect(computeCosts(home2400, SEED_CRITERIA).knownPersonalRecurring).toBe(122000);
expect(evaluateHome(home2400, SEED_CRITERIA, routes).fit).toBe('matches');
expect(evaluateHome(home2400, SEED_CRITERIA, routes).cost.unknownItems
  .some(x => x.key === 'electricity')).toBe(true);
expect(evaluateHome(home2400AndOneCent, SEED_CRITERIA, routes).constraints
  .find(x => x.key === 'personal_rent')?.outcome).toBe('fail');
expect(evaluateHome(oneAndHalfBaths, SEED_CRITERIA, routes).constraints
  .find(x => x.key === 'bathrooms')?.outcome).toBe('fail');
expect(evaluateHome(home2400, SEED_CRITERIA, routesOver1200).constraints
  .find(x => x.key === 'walk')?.outcome).toBe('fail');
```

- [ ] Implement `computeCosts`: rent basis/scope first; exact monthly base rent only; whole-household fees follow the declared split, per-person fees apply once, unknown allocation remains unresolved. Cap/usage charges remain unresolved. Never sum a utility charge twice via both charges and utility references. Keep one-time fees separate. Use rational/fixed-point comparisons before rounding.
- [ ] Implement `evaluateHome`: source-level hard facts, property/layout/rent/walk, explicitly selected included utilities and canonical must-have amenity booleans. Missing amenities are unknown, never false. Timing remains flexible display/readiness-only in v1; do not infer date filters from prose. Collect failures and unknowns, not a single opaque boolean. Derive readiness questions independently. Retain reported-off-market/historical exclusion and route destination-version checks.
- [ ] Implement `suggestAlternatives`: collect candidate rent/walk thresholds and bathroom minima; apply each patch with `applyCriteriaPatch`; evaluate through the same engine; report actual newly matched IDs. No invented counts and no counting a home still blocked by another hard criterion. Implement baseline/revert semantics without mutating the source criteria.
- [ ] Implement reconciliation with normalized address/building relation plus confirmed unit/offer identity. Preserve uncertain merges as separate options and conflicts as facts with alternatives. Write same-address/different-unit, per-room-vs-unit, and source-disappearance tests.
- [ ] Implement `needsDiscovery` in `src/domain/coverage.ts` using structured `ResearchScope`: changed market/destination, expanded layout/type/price/walk scope produce a reason. A reusable scope never hides source/page limits. Test a new city, a larger budget beyond the query cap, unchanged criteria and a changed destination version.
- [ ] Run the four domain test files plus typecheck once; hand off exact examples, unresolved assumptions and exported names. Root integrates. Only broaden tests for a discovered failure or later code change.

## Task 3: Real discovery, evidence capture and seed ingestion

**Owner:** Luna with root integration for CLI/process wrapper. **Depends on:** Task 1, Task 2 reconciliation when ready. This is the critical path. **Files:** `jobs/sources/*`, `jobs/ingest.ts`, `tests/ingest/*`, seed snapshots; root owns `server/cli.ts` and publication.

**Consumes:** registry, `Criteria`, public pages and optional imported text. **Produces:** `discover`, `fetchPublicPage`, schema-valid snapshots/evidence/source-run ledger, data provenance audit. Temporary captures are not final seed records.

- [ ] Populate the registry from the already completed source survey, including every major portal's distinct site name and family. Initialize statuses accurately from runtime research; don't label survey history as an app refresh. Include public manager leads at Kerpec, Lobos, Walnut, Reinhold and CMU. Check the specific public path/access conditions before adding a direct adapter; keep denied/restricted portals as links/gaps.
- [ ] Root implements a bounded CLI runner with argv arrays, `shell:false`, finite input/output buffers, child-process termination and abort. Claude discovery uses `--safe-mode`, WebSearch/WebFetch only, `--permission-mode dontAsk`, strict empty MCP config and no persistence. Model extraction uses no tools. Use validated Fable 5.1 for a small number of discovery calls; prefer validated OpenCode Go Luna for bounded text extraction after its schema-output handling is proven. Do not depend on a new model that has not returned a response.

```ts
const workerDirectory = path.join(process.cwd(), 'data', 'worker');
await fs.mkdir(workerDirectory, { recursive: true });
const approvedRuntimeEnvironment = Object.fromEntries(
  ['PATH', 'HOME', 'TMPDIR', 'LANG', 'XDG_CONFIG_HOME', 'XDG_DATA_HOME']
    .flatMap(key => process.env[key] === undefined ? [] : [[key, process.env[key]!]])
);
const child = spawn(binary, args, {
  cwd: workerDirectory, shell: false, stdio: ['pipe', 'pipe', 'pipe'],
  env: approvedRuntimeEnvironment
});
child.stdin.end(JSON.stringify(researchRequest));
// The wrapper caps stdout/stderr, aborts at 150 s, sends SIGTERM,
// then SIGKILL after 2 s if needed, and returns a structured terminal error.
```

Import Node `path`, `fs/promises` and `child_process.spawn`; `binary` and `args` come only from the fixed reviewer/discovery model configuration, and `researchRequest` is the validated structured criteria/public-source request. Do not forward unrelated GitHub tokens or host secrets to workers. CLI authentication uses its already validated local store. Ignore `data/worker` and captures/logs in Git.

- [ ] Implement `fetchPublicPage`: registered HTTPS hosts only; 15-second timeout, 12 MB streaming limit, redirect host/IP revalidation, two concurrent requests, no retry on access denial. Use cache and an identifying user agent. A blocked source creates a ledger entry, not a thrown failure for the entire batch.
- [ ] Implement source-specific selectors for parseable CMU/Lobos pages first. Preserve structured offer rows and title/address context; use a generic direct-manager parser only on captured evidence with demonstrable row boundaries. Hash captures and maintain `scopeKey` before extraction. Keep required source fields unknown when ambiguous.
- [ ] Write a regression fixture based on the observed Shadyside Commons structure: 1BR/1.5BA unit 482-0345 at $2,525 with Sept13; 2BR/2BA 482-0303 at $3,405 with Sept10. It is a labeled parser test, not an automatically current seed. A proposed swapped date must be rejected, even though the date occurs elsewhere in the page.

```ts
expect(twoBedroom.availability.value).toBe('2026-09-10');
expect(twoBedroom.availability.evidenceIds.every(id =>
  evidence.find(e => e.id === id)?.scopeKey === '482-0303'
)).toBe(true);
expect(validateExtractedFact(swappedDateProposal, captures).accepted).toBe(false);
```

`validateExtractedFact(proposal,captures)` is exported by `jobs/sources/extract.ts`; it returns `{accepted:boolean, reason:string|null}` after validating the proposed evidence reference, compatible record scope and a source-supported value. Matching a quote substring alone is insufficient.

- [ ] Run the first real discovery/import batch early. Target 18–30 useful options, minimum 12 addressed options/3 independent organizations; broad enough for informative near-matches. Collect rent basis, bath evidence and utilities deliberately. Maintain a source-run ledger for all sources attempted. Never turn a “starting at” building price into a specific unit quote or substitute old Craigslist snippets for current research.
- [ ] Conduct an agent-led five-record audit against captured source rows/URLs, including the multi-unit regression shape, one utility inclusion/cap, one per-room or range offer and one duplicate. Record corrected/unknown fields. Pass only valid data to atomic publication; quarantine failures and continue.
- [ ] Run a second bounded refresh using actual network retrieval. Record fetched timestamps and a data diff; no invented changes if the source content is unchanged. A fixture replay cannot satisfy this step. Keep last good snapshot if the source fails, with its previous observation date intact.

Run: `npm test -- tests/ingest`, `npm run ingest -- --market pittsburgh --limit 24`, `npm run ingest -- --market pittsburgh --limit 6 --refresh`. Implement the CLI options exactly as documented; its final JSON summary reports snapshot ID, fetched pages, imported IDs, quarantined fields and failures. Do not claim minimum seed acceptance until counted against the real snapshot.

## Task 4: Gates walking routes, useful buses and nearby essentials

**Owner:** Terra after Task 2. **Depends on:** contracts and first addressed seed. **Files:** `jobs/geo/*`, `tests/geo/{walk,transit,nearby}.test.ts`; route and dataset caches.

**Consumes:** `Snapshot`, `Destination`, official PRT GTFS, OSM place responses. **Produces:** `enrichRoutes`, enriched homes with `TransitContext`/`NearbyPlace`, source evidence and captured route geometry.

- [ ] Implement geocode-on-submit with cache and manual coordinate fallback; seed Gates directly from its saved mapped coordinate. Preserve coordinate evidence/mapped-entrance caveat. Validate candidate coordinates before routing and keep an unplaced lead when location is uncertain.
- [ ] Request the demonstrated foot endpoint with `overview=full&geometries=geojson&steps=true`. Map GeoJSON [lon,lat] correctly. Validate seconds/metres, step modes and <=75 m origin/destination snaps, then cache by coordinates/version/profile. Throttle to <=1 request/second; one transient retry, no parallel request burst.

```ts
const coordinates = `${origin.lon},${origin.lat};${destination.coordinate.lon},${destination.coordinate.lat}`;
const url = new URL(`https://routing.openstreetmap.de/routed-foot/route/v1/driving/${coordinates}`);
url.search = new URLSearchParams({ overview: 'full', geometries: 'geojson', steps: 'true' }).toString();
```

- [ ] Save at least 8 real seed route responses when locations permit; each route must terminate at the same versioned Gates destination. Failure creates an unavailable route object and no exact-walk qualification. Do not draw a radius/isochrone as an inferred walking boundary.
- [ ] Download the official PRT GTFS once with bounded streaming and parse required feed/calendar/calendar_dates/stops/routes/trips/stop_times data. Cache a compact campus/seed subset. Use America/New_York service dates and allow GTFS times beyond 24:00. Show a stated example weekday morning service window; do not describe it as a live arrival.
- [ ] Find nearby boarding stops; establish same-trip stop order to a campus-side stop before setting `servesDestination=true`. Include route number/headsign/stop/service date/window/feed version. Use nearest-stop straight-line distance with that label until separately routed; no false walk minutes. If proof is absent, show only nearby routes, not a commute promise.
- [ ] Query OSM groceries/pharmacies/cafes once per research area with a bounded result set. Preserve category/name/coordinate/source and report distance basis. Surface a few nearest useful essentials per home. OSM absence remains `not found in queried data`.
- [ ] Test destination version invalidation, 1200.1-second threshold, unavailable router, excessive snap, reverse-direction transit, service exception and after-midnight GTFS time. Check an actual route endpoint/geometry and known PRT stop sequence against the source artifacts. Add OSM/PRT attribution text from source documentation.

Run: `npm test -- tests/geo`, `npm run enrich -- --snapshot current --destination gates-hillman`. Expected: a new immutable snapshot, geographic evidence/route counts, errors without losing the original data.

## Task 5: API, snapshots and bounded jobs

**Owner:** Root while UI/data/domain lanes run. **Depends on:** contracts; plug in each producer when ready. **Files:** `server/*`, `tests/api/{search,jobs,snapshots}.test.ts`.

**Consumes/produces:** every API endpoint and error shape in CONTRACTS.md; `loadCurrentSnapshot`, `publishSnapshot`, `Job` states. No second implementation of matching.

- [ ] Implement snapshot storage: validate all evidence/route references, write immutable snapshot to temporary safe path, rename complete file, then atomically replace `data/current.json`. Read the pointer once per request. Keep all prior snapshots; a interrupted write cannot corrupt the serving one.
- [ ] Implement bootstrap/search, immutable `GET /api/snapshots/:id` and Express static serving. A job's snapshot is fully readable before its terminal success is published. Validate input with Zod, cap JSON to 100 KB and import text to 80 KB, enforce loopback/same-origin mutations. Arbitrary request strings never become subprocess commands or storage paths. Return typed errors with requestId and no private stack traces.
- [ ] Implement queued/running/terminal job state transitions and one active discovery job. Polling is 1 s while visible, slows/pauses when hidden and stops on terminal state. Progress advances only from real stages. Job wall-clock limit is 6 minutes; routes run with per-request bounds. Record errors once and preserve partial validated results/last good snapshot.
- [ ] Connect `POST /api/routes` to the geographic producer and `POST /api/discovery`/`import` to the same validated ingest pipeline. A second in-flight duplicate search returns its existing job; a different one returns a busy response with the current job ID. Do not launch uncontrolled subscription-consuming subprocesses per click.
- [ ] Exercise API/engine equality, malformed data, stale snapshot 409, failed import, timed-out model, source partial failure and interrupted publication. A test must prove a preexisting good snapshot remains readable after a simulated write failure. A second test completes a route/discovery job, reads its result through `/api/snapshots/:id` and verifies that exact snapshot is usable by the client.

```ts
expect(apiResult.results).toEqual(evaluateSearch(snapshot, criteria, requestId).results);
expect((await loadCurrentSnapshot()).id).toBe(beforeFailedPublish.id);
expect(terminalFailedJob.status).toBe('failed');
expect(terminalFailedJob.error?.code).toBe('MODEL_TIMEOUT');
```

Run: `npm test -- tests/api` then a real `GET /api/bootstrap`/`POST /api/search` round-trip with the seeded snapshot. API tests may use synthetic failures, but the round-trip proof uses the real seed.

## Task 6: Crafted list/map workspace and real criteria changes

**Owner:** Sol or Terra frontend worker; Fable reviews rendered work. **Depends on:** contracts and Task 1 shell; use clearly labeled synthetic development data until the first real snapshot, then replace before any user-facing acceptance.

**Files:** frontend paths from file map, `src/styles/{tokens,base,workspace}.css`, `public/fonts/`, `tests/browser/workspace.spec.ts` (root integrates test ownership).

**Consumes:** bootstrap snapshot/criteria, shared engine, all API contracts. **Produces:** real list/filter/map/detail loop; `SearchState` with baseline/current criteria, selection and snapshot IDs.

- [ ] Implement tokens and typography exactly as design starting point, including readable tabular prices/minutes and source metadata. Self-host font files with their license. Keep title, list/map ratio and density in presentation config/tokens. No generic filter-card dashboard, hero or invented match score.
- [ ] Build the desktop working header, editable assumption sentence, evidence strip and aligned two-line housing rows. Put whole-home rent and labeled personal calculation in one coherent cost block. Put utility names/status text in fixed order; source/time and readiness remain one click away. The derived household ceiling never contradicts the personal cap/split.
- [ ] Connect criteria edits to `applyCriteriaPatch` and the single engine; preserve baseline, selection and list scroll. Announce result-count change accessibly. Sync to API and discard obsolete responses by request/snapshot IDs. Explicit included-utility and amenity filters belong in More preferences; nice-to-haves do not become hidden hard constraints.
- [ ] Implement Leaflet imperatively inside a small React adapter with mount/update/cleanup boundaries. Numbered pins share selection state with rows. Gates is a named square. Selecting a row draws its cached computed route, dims others and shows profile/date; absent route and tiles have visible fallbacks. Clicking a pin scrolls/selects the corresponding row. Do not use a fake destination marker disconnected from the route data.
- [ ] Implement readable details within the list pane, retaining the map. Facts show compatible unit/building scope, conflicts, money breakdown, six utility statuses, bus context, essentials/amenities/reviews and verification questions. Use factual derived consequence sentences with source pointers, not runtime LLM prose.
- [ ] Implement alternatives from actual `Alternative` objects. Preview shows before/after criterion and newly eligible IDs; Apply changes actual state; Revert restores baseline; counts do not include homes still blocked elsewhere. Expanded discovery is explicit when the new search exceeds the researched market.
- [ ] Render first real-data core at 1440x1000 and 1280x800. Obtain Fable's first rendered review with screenshots and the changed behaviors; respond to the strongest visual/usability defects once, with root resolving conflicts against the user brief. No repeated palette churn. Preserve review artifact and before/after screenshots.

Visual checks: recognizable Gates endpoint; readable rent basis/split and utility uncertainty without opening a row; matching list/pin IDs; precise empty/unavailable states; no clipped costs. Functional browser checks: row→route→detail, pin→row, budget/split/walk edits, Apply/Revert, keyboard navigation and no console errors.

## Task 7: Comparison, shortlist, coverage and discovery in the interface

**Owner:** Frontend worker; root API integration. **Depends on:** Task 6 core and Task 5 endpoints. **Files:** `CompareSheet`, `ShortlistRail`, `CoveragePanel`, `SourceEvidence`, `DiscoveryStatus`, `src/state/search.ts` and narrow-layout CSS.

**Consumes:** `EvaluatedHome`, `CostSummary`, source ledger, jobs. **Produces:** all four accepted decision questions as working behavior, persistent local saved choices and transparent refresh.

- [ ] Save up to three homes for comparison in a rail; localStorage stores only IDs and scenario state. Compare aligned facts in one order: rent/share, recurring costs/unknowns, layout, utility statuses, walk, transit, errands, amenities, lease/availability, unanswered questions, source links. Retain out-of-filter favorites with a reason.
- [ ] Build the expandable coverage ledger: attempted/queried/fetched/imported/gaps, independent organizations, duplicate observations, exact observation window and search limits. Counts come from the snapshot; imported-home counts refer to IDs and never sum portal headline totals.
- [ ] Connect Find more homes and Refresh to bounded jobs with honest progress. Keep current list usable; surface partial/failure and old observation dates. On success fetch `/api/snapshots/:id` using the terminal job ID, validate/swap compatible snapshot, show actual diff, keep favorites/criteria, and reject obsolete destination results. URL/text import is accessible as fallback; it must not replace the app's discovery interaction.
- [ ] Implement destination editing: explicit geocode submit or map pin, new destination version, visible route recomputation, invalidation of old walk qualification. Different-city discovery carries the changed market. A lack of new-city data is an explicit empty/research state, never Pittsburgh records relabeled.
- [ ] Implement responsive behavior at 390x844: list/map switch, readable utility wrapping, no map/compare overlap, reachable close/back controls, focus restoration and reduced-motion support. Check status meanings without relying on color.
- [ ] Browser check the complete discovery→comparison→change→shortlist loop and persistence after reload. No new domain tests for cosmetic styling; test only the meaningful interactions and state transitions.

## Task 8: Independent review, acceptance and morning handoff

**Owner:** Root; bounded Fable frontend and OpenCode Go backend reviews. **Depends on:** integrated Tasks 1–7. **Files:** `docs/{verification,runbook,demo}.md`, `docs/reviews/*`, `scripts/demo-reset.ts`, browser tests and screenshot artifacts.

- [ ] Run the minimum release checks against the final revision and real snapshot: `npm run typecheck`, `npm test`, `npm run build`, `npm run test:browser`. Record exact commands, exit results, revision/snapshot IDs and meaningful failures fixed. Do not list tests as passed from a prior different revision.
- [ ] Cold-start the compiled app with `npm start` and verify the seeded list, rent basis, utilities, route details, comparisons, baseline/revert and saved IDs. Temporarily disable model access: core still works. Block a new route: qualification becomes unknown. Fail a source refresh: last good snapshot survives and coverage reports failure. Restore normal configuration afterward.
- [ ] Audit five real records and actual seed counts against URLs/captured evidence; ensure at least one unit-table association is inspected. Confirm any exact-match claim has known rent/layout/type/walk facts. Unknown utility bills remain prominent without incorrectly failing rent-only matching. If there are zero matches, use the honest zero-match/near-match story.
- [ ] Send Fable the final rendered core and specific comparison/empty/narrow states. Send OpenCode Go a focused backend packet with contract, relevant code, test/failure results and known gaps. These are continuations of the user-requested reviewers; keep credentials/private host context out. Implement material corrections with one owner; a reviewer does not silently change product scope.
- [ ] Produce `docs/runbook.md`: exact install/build/start URL, local discovery capability dependency, reset command, fixed seed snapshot ID, logs, source limitations, how to change tokens/layout, and how to restart after a crashed process. `demo:reset` restores the versioned seed criteria and seed pointer without deleting newer snapshots or favorite data; provide a separate UI reset for local demo state.
- [ ] Keep the local machine/app available during the authorized overnight build using a task-owned process and, if supported, `caffeinate` tied to that process. Record PID/start command and avoid a detached uncontrolled agent loop. This protects against idle sleep while running; it is not a claim to wake a powered-off machine or guarantee execution after the agent session ends.
- [ ] Write the three-minute demo script and morning handoff. Show the source coverage and criterion set immediately, one evidence/cost comparison, a selected route to Gates, one actual changed-input result and the readiness questions. Name the inventory snapshot and limitations; do not spend the demo watching a multi-minute crawl. Show a completed real refresh/diff, or start a bounded background refresh while continuing the comparison.
- [ ] At final handoff leave a usable local candidate, source evidence, screenshot artifacts and exact known gaps. Do not mark completion from a screenshot, a green build or panel agreement alone. Do not publish externally or contact housing providers as a side effect of demo preparation.

## Requirements coverage and review checklist

| Conversation IDs | Implemented through | Acceptance evidence |
| --- | --- | --- |
| C01, C17 | milestone clock; Task 8 | morning runnable candidate and three-minute script |
| C02, C03 | Tasks 3, 5, 7 | all major sources surveyed; real bounded discovery; changed-market behavior; accurate gaps |
| C04, C05 | Tasks 2, 6, 7 | coverage/consequences/alternatives/readiness all visible and computed |
| C06, C09 | Tasks 1, 3, 8 | real seeded records, scope/evidence audit, truthful unknowns/zero matches |
| C07, C08 | Tasks 6, 7, 8 | browsable crafted list/map, responsive screenshots and Fable reviews |
| C10, C13 | Tasks 2, 4, 6 | 2BR/2+bath rules, raw-second foot threshold, named Gates endpoint used by route/filter |
| C11, C12 | Tasks 1, 2, 6, 7 | labeled personal split, distinct rent/fees/utilities, unknown-not-zero tests |
| C14, C15 | Tasks 4, 6, 7 | relevant PRT direction/order, sourced nearby context, amenities and review scope |
| C16 | Tasks 1, 2, 6 | flexible seed timing with availability/lease evidence retained |
| C18, C19, C20 | runtime validation; ownership; Tasks 3, 5, 8 | actual model invocations, bounded work/retries, dedicated reviews and preserved candidate |
| C21, C22 | archived conversation; spec/contract; task ownership | one canonical plan/engine; no redundant task service or compulsory human component roles |

Before reporting the plan ready, root checks: every design section has an implementation task; function/type names match CONTRACTS.md; required criteria survive into seed/tests/UI; no placeholders or unbounded retry loops; no mistaken claim that planning probes are completed application features. The independent contract review is recorded separately and material findings are reconciled before go.

Root planning check completed September 12, 2026: C01–C22 mapped; substantive Fable/OpenCode/Terra findings resolved in `docs/reviews/RESOLUTIONS.md`; relative document links checked; canonical units/seed/global constraints compared. No application test/build result is claimed at planning time.
